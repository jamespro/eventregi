import PlainForm from '../components/misc/PlainForm';

const Reginfo = () => {
    return (
        <section>
            <h1>Contact Info</h1>
            <PlainForm />
        </section>
    )
};

export default Reginfo;
